# CAP dogfood fixture

This directory is a target-project fixture, not the CAP control plane. Initialize it as a Git repository, commit the fixture, then register `.acceptance/project.yaml` and submit the commit through the CLI. The commands are intentionally deterministic and do not need a device, browser, network, or Codex credential.
