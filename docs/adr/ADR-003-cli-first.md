# ADR-003: CLI first

## Decision

The first user interface is a scriptable CLI. Dashboard and HTTP surfaces remain later adapters over services.

## Consequences

The acceptance loop can be exercised in local and CI environments before UI work begins.
